# 🧠 OCRtoODT — интеллектуальное распознавание текста и конвертация в ODT

**OCRtoODT** — это современное кроссплатформенное приложение для распознавания текста на изображениях и PDF-документах с последующим экспортом в редактируемый формат **.odt** (LibreOffice / OpenOffice). Проект создан как открытая альтернатива коммерческим системам OCR (например, ABBYY FineReader) — лёгкая, удобная и полностью автономная.

---

## 🚀 Основные возможности
- 📄 Конвертация сканированных PDF и изображений (JPEG, PNG, TIFF) в редактируемые ODT-документы.
- 🧠 Распознавание текста с помощью встроенного **Tesseract OCR**.
- ⚙️ Настраиваемая предварительная обработка изображений (ч/б, повышение контрастности, удаление шумов и т.д.).
- 🪶 Автоматическое определение структуры документа и абзацев.
- 🌗 Поддержка светлой и тёмной темы интерфейса.
- 🖱️ Полностью графический интерфейс (PySide6 / Qt6) — никаких консольных команд.
- 💾 Поддержка сохранения параметров в **config.yaml** с сохранением комментариев.
- 🔄 Параллельная обработка страниц (многопроцессорная загрузка).
- 💬 Многоязычное OCR (русский, английский и др.).

---

## 🧩 Архитектура проекта
Проект построен по модульному принципу и разделён на уровни обработки данных:

```
ocrtoodt/
 ├── i0_core/                # Основная логика и управление конвейером
 │   ├── cli_entrypoint.py   # CLI-точка входа для автоматической обработки
 │   ├── pipeline_orchestrator.py  # Управление всем процессом OCR
 │   ├── pdf_splitter.py     # Разделение PDF на изображения
 │   └── types_definitions.py # Определения структур данных
 │
 ├── i1_preprocess/          # Модули предварительной обработки изображений
 │   ├── grayscale.py, denoise_median.py, contrast_clahe.py, binarize_otsu.py
 │   └── image_preprocessor.py
 │
 ├── i2_ocr/                 # Слой распознавания текста (Tesseract)
 │   └── ocr_engine.py
 │
 ├── i3_lines_analysis/      # Анализ строк и определение структуры абзацев
 │   └── lines_classifier.py
 │
 ├── i4_document_builder/    # Формирование итогового ODT-документа
 │   └── odt_assembler.py
```

GUI находится в папке `gui/` и реализован на **PySide6**:
```
gui/
 ├── main.py                 # Точка входа GUI
 ├── dialogs/                # Диалоги: настройки, помощь, о программе
 ├── resources/              # Иконки, темы, звуки
 ├── ui/                     # Qt Designer файлы (.ui)
 └── theme.py, worker.py     # Управление интерфейсом и потоками
```

---

## 🖥️ Интерфейс пользователя
Главное окно программы напоминает классические офисные OCR-приложения:

### 🔘 Основные элементы:
- **Кнопка «Открыть»** — загрузка PDF или изображений.
- **Кнопка «Распознать»** — запуск процесса OCR.
- **Кнопка «Экспортировать»** — сохранение результата в формате ODT.
- **Кнопка «Настройки»** — открывает окно конфигурации.
- **Полоса прогресса** и **журнал событий** — показывают состояние распознавания.

### ⚙️ Диалог «Настройки» содержит вкладки:
1. **Общие** — выбор папки входных файлов, включение многопроцессорной обработки (auto/1–32 потоков).
2. **Предобработка** — включение фильтров (шумоподавление, контраст, бинаризация, ч/б, резкость).
3. **OCR** — выбор языков и параметров Tesseract (PSM, DPI, tessdata).
4. **ODT** — настройка форматирования абзацев, шрифтов и отступов.
5. **Интерфейс** — выбор темы (светлая/тёмная), размера шрифта, уведомлений и звука завершения.

---

## ⚙️ Конфигурация (config.yaml)
Все настройки сохраняются в `config.yaml`. Комментарии сохраняются благодаря использованию **ruamel.yaml**.

Пример:
```yaml
program_root: /home/user/OCRtoODT
input_dir: input
output_file: output/result.odt
ocr_engine_path: tesseract/tesseract
languages: [rus, eng]
psm: 4
parallel:
  enabled: true
  num_processes: auto
```

---

## 🔧 Установка и запуск
### Из исходников (разработка)
```bash
git clone https://github.com/Rostislav62/OCRtoODT.git
cd OCRtoODT
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python gui/main.py
```

---

## 🔍 Технические детали
- **Язык**: Python 3.12
- **GUI**: PySide6 / Qt6
- **OCR**: Tesseract (встроенный)
- **ODT**: odfpy
- **PDF обработка**: PyMuPDF (fitz), pdf2image
- **Предобработка**: OpenCV + NumPy
- **Конфигурация**: ruamel.yaml (с сохранением комментариев)

---

## 🧑‍💻 Автор и лицензия
Автор: **Ростислав Смиглюк**  
Лицензия: [MIT License](LICENSE)
